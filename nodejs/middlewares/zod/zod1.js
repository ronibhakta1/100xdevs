// if this is an array string with atleast 1 input, return true, else return false 
// this is an example that with using zod howmany checks we needed to full fill to get the right output from the front-end 


function validateInput(arr){
    if (typeofarr == 'object' && arr.length >=1 && typeof arr[0] == 'number' && arr.every((item)=> typeof item == 'number')){

    }
}

// the better way is in the zod2.js file 
